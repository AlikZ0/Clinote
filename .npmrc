# Keep dependency resolution strict: a package must declare what it imports.
auto-install-peers=true
strict-peer-dependencies=false
# Never run install scripts from arbitrary transitive dependencies (security.md §10).
enable-pre-post-scripts=false
